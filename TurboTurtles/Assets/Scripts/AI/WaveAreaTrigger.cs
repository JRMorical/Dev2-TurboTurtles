/*using UnityEngine;

public class WaveAreaTrigger : MonoBehaviour
{
    [SerializeField] WaveAreaController areaToStart;

    private void OnTriggerEnter(Collider other)
    {
        if (!other.CompareTag("Player"))
            return;

        if (areaToStart != null)
            areaToStart.StartArea();
    }
}
*/